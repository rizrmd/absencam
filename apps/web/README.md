# Absencam web

Vite + React + TypeScript SPA with shadcn/ui and Zustand.

See the repo root [README.md](../../README.md) and [AGENTS.md](../../AGENTS.md).

```bash
npm install
npm run dev      # :5173, proxies /api → :8080
npm run build
```
